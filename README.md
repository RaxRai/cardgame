frontent-> react
backend-> node.js

setup -> connect to mongo db database

to start use frontend command:

                cd frontend
                npm install
                npm start

to start use backend command:

                cd backend
                npm install
                node server.js
